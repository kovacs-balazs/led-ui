import { TypeLedStrip } from "@/types/types";

export type LedStripPropertiesProps = {
  ledStrip: TypeLedStrip;
  // onUpdate: (changes: Partial<LedStrip>) => void;
};


export default function LedStripProperties({ ledStrip }: LedStripPropertiesProps) {
  
}